## Actor & Critic

### 🗺️ План Ответа

1.  **Общий Обзор:** Краткое введение в архитектуру **Актор-Критик**.
2.  **🤖 Класс `Actor` (Политика):** Построчный анализ архитектуры и методов.
    -   `__init__`: Инициализация нейронной сети.
    -   `forward`: Вычисление параметров для стохастического действия.
    -   `sample`: Генерация действия и его логарифмической вероятности с использованием **Reparameterization Trick**.
3.  **🧐 Класс `Critic` (Оценка):** Построчный анализ архитектуры и методов.
    -   `__init__`: Инициализация сети-оценщика.
    -   `forward`: Вычисление ценности (Q-value) для пары "состояние-действие".
4.  **💎 Ключевой Вывод:** Синтез информации и общая картина работы системы.

---

### 🤖 Класс `Actor` (Политика)

`Actor` — это нейронная сеть, реализующая *политику* (policy) агента. Ее задача — на основе текущего состояния среды (`state`) решить, какое *действие* (`action`) предпринять. В данном случае реализуется стохастическая политика для непрерывного пространства действий.

#### `__init__(self, d_model: int, action_dim: int)`
Метод-конструктор, который определяет архитектуру сети. Выполняется один раз при создании объекта класса.

| Строка | Объяснение |
|---|---|
| `class Actor(nn.Module):` | Объявляет класс `Actor`, который наследуется от базового класса всех нейросетевых модулей в **PyTorch** — `nn.Module`. Это позволяет классу отслеживать свои параметры (веса) и легко интегрироваться в экосистему `PyTorch`. |
| `super().__init__()` | Обязательный вызов конструктора родительского класса `nn.Module`. Он выполняет необходимую внутреннюю инициализацию. |
| `self.net = nn.Sequential(...)` | Создается "тело" сети. `nn.Sequential` — это контейнер, который последовательно применяет вложенные в него слои. |
| `nn.Linear(d_model, d_model // 2)` | Первый полносвязный слой. Он принимает на вход вектор размерности `d_model` (размерность эмбеддинга состояния) и преобразует его в вектор размерности `d_model // 2`. |
| `nn.ReLU()` | Функция активации (Rectified Linear Unit). Вносит нелинейность, позволяя сети аппроксимировать сложные функции. Обнуляет все отрицательные значения, пропуская положительные без изменений. |
| `nn.Linear(d_model // 2, d_model // 4)` | Второй полносвязный слой, который еще больше сжимает представление. |
| `self.mu_head = nn.Linear(...)` | "Голова" для вычисления **μ (mu)** — математического ожидания (среднего) Гауссова распределения. На выходе — вектор размерности `action_dim`, где каждый элемент — это среднее для одного из измерений действия. |
| `self.log_std_head = nn.Linear(...)` | "Голова" для вычисления **логарифма стандартного отклонения (log σ)**. Сеть учится предсказывать логарифм, а не само стандартное отклонение, по двум причинам: 1) `log(σ)` может принимать любые действительные значения, в то время как `σ` должно быть > 0, что упрощает оптимизацию. 2) Экспоненцирование `log(σ)` является более численно стабильной операцией, чем, например, использование `softplus` для обеспечения положительности. |

#### `forward(self, latent_state: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]`
Этот метод определяет прямой проход данных через сеть. Он не генерирует само действие, а вычисляет параметры (`mu` и `log_std`), которые его описывают.

| Строка | Объяснение |
|---|---|
| `aggregated_state = latent_state.mean(dim=1)` | Ключевая операция для данной задачи. Входной тензор `latent_state` имеет размерность `[batch_size, n_comps, d_model]`, где `n_comps` — это, вероятно, количество активов в портфеле. Эта строка усредняет векторы всех активов, получая единый вектор состояния портфеля размерностью `[batch_size, d_model]`. Это упрощение, позволяющее принимать одно глобальное решение, а не отдельные для каждого актива. |
| `x = self.net(aggregated_state)` | Усредненное состояние пропускается через "тело" сети (MLP), чтобы извлечь признаки более высокого уровня. |
| `mu = self.mu_head(x)` | Полученные признаки подаются на "голову" `mu`, чтобы вычислить среднее для распределения действий. |
| `log_std = self.log_std_head(x)` | Параллельно те же признаки подаются на "голову" `log_std` для вычисления логарифма стандартного отклонения. |
| `log_std = torch.clamp(log_std, min=-20, max=2)` | **Критически важная строка для стабильности обучения.** Она ограничивает значение `log_std` в заданном диапазоне. **Почему?** Если `log_std` станет слишком большим, `std` (дисперсия) "взорвется", и агент будет совершать хаотичные, огромные по амплитуде действия. Если `log_std` станет слишком маленьким (например, -∞), `std` устремится к нулю, политика станет детерминированной, и агент прекратит исследование (exploration) среды. Диапазон `[-20, 2]` — эмпирически подобранные значения, используемые во многих реализациях SAC. |
| `return mu, log_std` | Метод возвращает параметры, описывающие распределение вероятностей для действий. |

#### `sample(self, latent_state: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]`
Этот метод использует параметры из `forward` для генерации конкретного действия и вычисления его логарифмической вероятности.

| Строка | Объяснение |
|---|---|
| `mu, log_std = self.forward(latent_state)` | Сначала получаем параметры распределения. |
| `std = log_std.exp()` | Вычисляем стандартное отклонение, беря экспоненту от логарифма. `exp(log(σ)) = σ`. |
| `normal = Normal(mu, std)` | Создаем объект Гауссова (нормального) распределения с вычисленными параметрами `mu` и `std`. |
| `x_t = normal.rsample()` | **Центральный элемент — Reparameterization Trick.** Вместо прямого сэмплирования `z ~ N(μ, σ)`, которое является стохастической операцией и не позволяет градиентам проходить "сквозь" себя, трюк переформулирует его как `z = μ + σ * ε`, где `ε ~ N(0, 1)`. `rsample()` реализует именно это. Теперь стохастичность "вынесена" в `ε`, а путь от `μ` и `σ` до `z` становится дифференцируемым, что позволяет обучать нейросеть методом градиентного спуска. |
| `action = torch.tanh(x_t)` | Применение гиперболического тангенса к сэмплированному значению. `tanh` сжимает неограниченный выход Гауссова распределения в диапазон `[-1, 1]`. Это стандартная практика для сред, где действия ограничены (например, усилие мотора от -100% до +100%). |
| `log_prob = normal.log_prob(x_t)` | Вычисляем логарифм вероятности сэмплированного значения `x_t` в исходном нормальном распределении. |
| `log_prob -= torch.log(1 - action.pow(2) + 1e-6)` | **Математическая коррекция, характерная для алгоритма SAC.** Когда мы преобразуем случайную величину `x_t` с помощью функции `tanh`, плотность ее распределения меняется. Эта строка корректирует `log_prob`, чтобы он соответствовал вероятности конечного действия `action`, а не промежуточного `x_t`. Формула происходит из правила замены переменных в плотностях вероятности. `1e-6` добавлено для численной стабильности, чтобы избежать `log(0)`. |
| `log_prob = log_prob.sum(1, keepdim=True)` | Если `action_dim > 1` (многомерное действие), логарифмические вероятности по каждой из компонент суммируются, чтобы получить общую `log_prob` для всего вектора действия (предполагая независимость компонент). |
| `return action, log_prob` | Возвращается сгенерированное действие (готовое к отправке в среду) и его корректная логарифмическая вероятность (необходимая для вычисления функции потерь). |

---

### 🧐 Класс `Critic` (Оценка)

`Critic` — это нейронная сеть, реализующая *функцию ценности действия* (Q-function). Ее задача — оценить, насколько "хорошим" является совершение определенного действия (`action`) в определенном состоянии (`state`). Она предсказывает ожидаемое будущее вознаграждение (Q-value).

#### `__init__(self, d_model: int, action_dim: int)`
Конструктор, определяющий архитектуру сети Критика.

| Строка | Объяснение |
|---|---|
| `class Critic(nn.Module):` | Аналогично `Actor`, наследуется от `nn.Module`. |
| `self.net = nn.Sequential(...)` | Создается тело сети Критика. |
| `nn.Linear(d_model + action_dim, ...)` | **Ключевое отличие от Актора.** Критик оценивает пару `(состояние, действие)`. Поэтому на вход первому слою подается конкатенированный (объединенный) вектор состояния (`d_model`) и действия (`action_dim`). |
| `nn.Linear(d_model // 4, 1)` | Последний слой сети. Он выдает **одно-единственное число**. Это и есть Q-value — скалярная оценка "качества" входной пары (состояние, действие). |

#### `forward(self, latent_state: torch.Tensor, action: torch.Tensor) -> torch.Tensor`
Метод, вычисляющий Q-value.

| Строка | Объяснение |
|---|---|
| `aggregated_state = latent_state.mean(dim=1)` | Состояние агрегируется точно так же, как и в `Actor`. **Это важно для консистентности:** обе сети должны "видеть" состояние в одном и том же формате. |
| `x = torch.cat([aggregated_state, action], dim=1)` | Вектор агрегированного состояния и вектор действия объединяются в один длинный вектор. `dim=1` указывает, что конкатенация происходит по оси признаков (вдоль "ширины" батча). |
| `return self.net(x)` | Объединенный вектор пропускается через сеть Критика, которая на выходе выдает предсказанное Q-value. |

---

### 💎 Ключевой Вывод

Представленный код реализует архитектуру **Актор-Критик** для задачи обучения с подкреплением в непрерывном пространстве действий.

-   **`Actor` (Агент)**: Использует стохастическую политику, параметризованную Гауссовым распределением. Он предлагает действие, сжимая его в диапазон `[-1, 1]` с помощью `tanh`. Применение **Reparameterization Trick** и последующая коррекция логарифмической вероятности являются отличительными чертами современных и эффективных алгоритмов, таких как **Soft Actor-Critic (SAC)**.
-   **`Critic` (Оценщик)**: Оценивает предложенное Актором действие в текущем состоянии. Он учится предсказывать будущую суммарную награду (Q-value).

Вместе они формируют цикл обучения: `Actor` исследует среду, предлагая действия, а `Critic` предоставляет обратную связь, говоря, какие из этих действий были "хорошими", а какие "плохими". Эта обратная связь используется для обновления весов обеих сетей, что приводит к постепенному улучшению политики `Actor`.

---

### 💰 Бизнес-Контекст

В контексте управления активами, который угадывается по коду (`latent_state.mean(dim=1)`), эта система может работать следующим образом:
-   **Состояние (`latent_state`)**: Векторные представления (эмбеддинги) для каждого актива в портфеле, содержащие информацию о его цене, волатильности, новостном фоне и т.д.
-   **Действие (`action`)**: Вектор размерности `action_dim`, где каждый элемент (в диапазоне `[-1, 1]`) может означать изменение доли актива в портфеле. Например, `+1` — максимально увеличить долю, `-1` — продать актив, `0` — не изменять.
-   **Q-value (выход Критика)**: Предсказание будущей доходности (или другой целевой метрики, например, Шарпа) портфеля, если будет совершено данное действие `action`.
-   **Цель обучения**: Найти такую политику (`Actor`), которая максимизирует предсказания Критика, то есть максимизирует ожидаемую доходность портфеля.

Такая система позволяет создать полностью автоматизированного агента для алгоритмической торговли или динамической ребалансировки портфеля.

---

### 💡 Катализатор Знаний

-   **Энтропия в SAC**: Алгоритм **Soft Actor-Critic (SAC)**, на который похож ваш код, добавляет в функцию потерь дополнительный член — энтропию политики. Это поощряет `Actor` поддерживать максимально возможную случайность (высокую энтропию) при достижении цели. Это делает обучение более стабильным и способствует лучшему исследованию среды.
-   **Twin-Delayed DDPG (TD3)**: Для борьбы с переоценкой Q-значений (когда Критик становится излишне "оптимистичным"), в современных алгоритмах часто используют двух Критиков (`Twin Critic`). При обновлении используется минимальное из двух Q-значений, что делает оценку более консервативной и робастной.
-   **Детерминированная политика**: В некоторых алгоритмах (например, DDPG) `Actor` реализует детерминированную политику — для одного и того же состояния он всегда выдает одно и то же действие. Исследование (exploration) в таких случаях добавляется искусственно, например, путем прибавления шума к действию. Стохастическая политика, как в вашем коде, является более современным и интегрированным подходом к исследованию.

---

### 📚 Векторы Верификации

-   **Актор-Критик (Actor-Critic methods):** [Статья в Wikipedia](https://en.wikipedia.org/wiki/Actor%E2%80%93critic_method)
-   **PyTorch (`nn.Module`):** [Официальная документация PyTorch](https://pytorch.org/docs/stable/generated/torch.nn.Module.html)
-   **Reparameterization Trick:** [Статья D. Kingma, M. Welling (2013), "Auto-Encoding Variational Bayes"](https://arxiv.org/abs/1312.6114) — первоисточник для VAE, где трюк был популяризирован.
-   **Soft Actor-Critic (SAC):** [Оригинальная статья от исследователей из Berkeley/Google](https://arxiv.org/abs/1801.01290) и ее [вторая, улучшенная версия](https://arxiv.org/abs/1812.05905), где используется автоматическая настройка веса энтропии.
