## TransformerFeatureExtractor

### 🗺️ План Ответа

1.  **#️⃣ Общая Архитектура и Цель:** Разберем с высоты птичьего полета, какую задачу решает этот модуль.
2.  **⚙️ Деконструкция `__init__`:** Построчный анализ метода инициализации, где создаются все "строительные блоки" модели.
3.  **➡️ Деконструкция `forward`:** Пошаговый анализ потока данных через модуль, от "сырых" признаков до контекстуализированных эмбеддингов.
4.  **🤝 Синтез: Как это работает вместе?** Объединим все части, чтобы увидеть целостную картину работы механизма.

---

### #️⃣ Общая Архитектура и Цель

С точки зрения **первых принципов**, любая сложная система (рынок, текст, изображение) состоит из взаимодействующих элементов. Задача модели — не просто понять каждый элемент в отдельности, а уловить **контекст**, то есть то, как элементы влияют друг на друга.

Классические нейронные сети (MLP) обрабатывают каждый элемент (актив) изолированно. Рекуррентные сети (RNN/LSTM) обрабатывают их последовательно, что вносит искажения. **Архитектура Трансформера** решает эту проблему фундаментально: она позволяет каждому элементу "посмотреть" на все остальные элементы в наборе *одновременно* и взвесить их важность.

`TransformerFeatureExtractor` реализует эту идею для портфеля активов. Его цель — на вход получить матрицу `[количество_активов, количество_признаков_на_актив]` и на выходе выдать матрицу `[количество_активов, размер_эмбеддинга]`, где каждый выходной вектор (эмбеддинг) для актива `i` содержит информацию не только о самом активе `i`, но и о его взаимосвязях со всеми остальными активами в портфеле. Это и есть "извлечение сути" или "контекстуализация".

### ⚙️ Деконструкция `__init__`

Метод `__init__` — это конструктор класса. Здесь мы заранее создаем и инициализируем все слои нейронной сети, которые будут использоваться для обработки данных. Это похоже на заготовку всех необходимых станков на заводе перед запуском конвейера.

> ```python
> class TransformerFeatureExtractor(nn.Module):
> ```

-   **Что это:** Объявление класса `TransformerFeatureExtractor`, который наследуется от `torch.nn.Module`.
-   **Почему так:** `nn.Module` — это базовый класс для всех нейросетевых модулей в PyTorch. Наследование от него дает нашему классу массу полезных возможностей: отслеживание всех внутренних слоев и их параметров, легкое переключение между режимами обучения и оценки (`.train()`, `.eval()`), возможность перемещения модели на GPU/CPU (`.to(device)`) и т.д. Это фундаментальный паттерн проектирования в **PyTorch**.

> ```python
> def __init__(self, n_comps: int, n_features: int, d_model: int, n_head: int, num_layers: int):
>     super().__init__()
> ```

-   **Что это:** Конструктор класса с параметрами и вызов конструктора родительского класса.
-   **Почему так:** `super().__init__()` — обязательная строка. Она вызывает конструктор `nn.Module`, который настраивает всю внутреннюю "магию" PyTorch. Без этого вызова ваша модель не будет работать корректно.
-   **Параметры:**
    | Параметр | Тип | Описание |
    |---|---|---|
    | `n_comps` | `int` | Количество компонентов (активов) в портфеле. |
    | `n_features` | `int` | Количество "сырых" признаков для каждого актива (цена, объем, волатильность и т.д.). |
    | `d_model` | `int` | Внутренний размер векторов (эмбеддингов) модели. Это "рабочее пространство" трансформера. |
    | `n_head` | `int` | Количество "голов" внимания (attention heads). |
    | `num_layers` | `int` | Количество слоев энкодера, уложенных друг на друга. |

> ```python
> self.input_projection = nn.Linear(n_features, d_model)
> ```

-   **Что это:** Создание линейного (полносвязного) слоя.
-   **Почему так:** "Сырые" признаки (`n_features`) могут иметь любую размерность. Трансформер же работает с векторами фиксированной размерности `d_model`. Этот слой служит "переводчиком" или "входными воротами": он преобразует (проецирует) вектор признаков каждого актива в это стандартизированное внутреннее пространство модели. Это позволяет модели начать работать с данными в удобном для нее формате.

> ```python
> self.pos_encoder = PositionalEncoding(d_model, max_len=n_comps)
> ```

-   **Что это:** Создание модуля позиционного кодирования. (Класс `PositionalEncoding` здесь не показан, но это стандартная реализация).
-   **Почему так:** Механизм self-attention по своей природе инвариантен к порядку элементов. Для него набор `[A, B, C]` — это то же самое, что и `[C, B, A]`. В некоторых задачах (как в анализе предложений) порядок критичен. В анализе портфеля активов он может быть менее важен, но все же полезен (например, если активы отсортированы по капитализации). **Позиционное кодирование** добавляет к эмбеддингу каждого актива уникальный "сигнал", зависящий от его позиции в последовательности. Это позволяет модели различать первый актив от пятого, даже если их признаки идентичны.

> ```python
> encoder_layers = nn.TransformerEncoderLayer(d_model, n_head, dim_feedforward=d_model*4, batch_first=True)
> ```

-   **Что это:** Создание одного слоя трансформер-энкодера. Это сердце всей архитектуры.
-   **Почему так:** Один слой энкодера выполняет две ключевые операции:
    1.  **Multi-Head Self-Attention:** Та самая "магия", где каждый актив "смотрит" на все остальные и вычисляет контекстуализированный вектор. `n_head` — это количество независимых "взглядов" или "суб-пространств", в которых происходит этот анализ. Это позволяет модели улавливать разные типы зависимостей.
    2.  **Feed-Forward Network:** Обычная двухслойная полносвязная сеть, которая применяется к выходу self-attention для дополнительной обработки. `dim_feedforward=d_model*4` — это стандартная эвристика из оригинальной статьи, которая эмпирически хорошо работает.
-   `batch_first=True`: Этот флаг указывает, что наши тензоры будут иметь формат `[размер_батча, длина_последовательности, размер_признаков]`. Это более интуитивный формат, но исторически PyTorch Transformer ожидал `[длина_последовательности, размер_батча, ...]`. Мы увидим последствия этого в методе `forward`.

> ```python
> self.transformer_encoder = nn.TransformerEncoder(encoder_layers, num_layers)
> ```

-   **Что это:** Создание "стопки" из нескольких одинаковых слоев энкодера.
-   **Почему так:** Один слой энкодера может уловить только один уровень взаимосвязей. Укладывая `num_layers` слоев друг на друга, мы позволяем модели строить все более сложные и абстрактные представления. Выход первого слоя становится входом для второго, и так далее. Это позволяет модели находить зависимости второго, третьего и более высоких порядков.

### ➡️ Деконструкция `forward`

Метод `forward` определяет "конвейер" — как именно данные проходят через слои, объявленные в `__init__`.

> ```python
> def forward(self, x: torch.Tensor) -> torch.Tensor:
>     # x: [batch_size, n_comps, n_features]
> ```

-   **Что это:** Определение прямого прохода. `x` — это входной тензор.
-   **Почему так:** В PyTorch вы не вызываете `model.forward(data)` напрямую. Вы пишете `model(data)`. `nn.Module` перехватывает этот вызов и выполняет код внутри `forward`, добавляя необходимые служебные операции.
-   **Формат `x`:** `batch_size` — количество примеров (портфелей), обрабатываемых за раз. `n_comps` — количество активов в одном портфеле. `n_features` — количество признаков для каждого актива.

> ```python
> x = self.input_projection(x) # -> [batch_size, n_comps, d_model]
> ```

-   **Что это:** Применение входного линейного слоя к данным.
-   **Почему так:** Каждый вектор из `n_features` признаков для каждого актива в каждом примере из батча преобразуется в вектор размерности `d_model`. `nn.Linear` автоматически применяется к последней размерности тензора.

> ```python
> x = x.permute(1, 0, 2) # -> [n_comps, batch_size, d_model]
> ```

-   **Что это:** Перестановка размерностей тензора. 0-я становится 1-й, 1-я — 0-й, 2-я остается на месте.
-   **Почему так:** Это ключевой и часто сбивающий с толку момент. Несмотря на то, что мы указали `batch_first=True` в `TransformerEncoderLayer`, сам "оберточный" класс `nn.TransformerEncoder` *игнорирует* этот флаг и по-прежнему ожидает на вход данные в формате `[seq_len, batch_size, features]`. Это несоответствие в API PyTorch. Поэтому мы вынуждены вручную менять порядок размерностей перед подачей данных в энкодер.

> ```python
> x = self.pos_encoder(x)
> ```

-   **Что это:** Добавление позиционной информации.
-   **Почему так:** К каждому вектору в `x` прибавляется вектор позиционного кодирования. Теперь модель имеет информацию как о содержании актива (из `input_projection`), так и о его положении в портфеле.

> ```python
> output = self.transformer_encoder(x) # -> [n_comps, batch_size, d_model]
> ```

-   **Что это:** Прогон данных через всю стопку трансформер-энкодеров.
-   **Почему так:** Здесь и происходит основная работа. Данные проходят через `num_layers` слоев, на каждом из которых происходит self-attention и feed-forward обработка. На выходе мы получаем тензор той же размерности, но теперь каждый вектор для актива "обогащен" контекстом обо всех остальных активах.

> ```python
> output = output.permute(1, 0, 2) # -> [batch_size, n_comps, d_model]
> ```

-   **Что это:** Обратная перестановка размерностей.
-   **Почему так:** Мы возвращаем тензор к интуитивно понятному формату `batch_first`, с которым обычно работают последующие слои модели (например, слой принятия решений). Это хорошая практика — модуль должен возвращать данные в том же формате, в котором он их ожидает на входе (если это возможно).

### 🤝 Синтез: Как это работает вместе?

1.  **Вход:** Набор векторов, описывающих активы в портфеле. Каждый вектор "не знает" о других.
2.  **Проекция:** Векторы переводятся на "внутренний язык" модели (пространство `d_model`).
3.  **Кодирование Позиции:** К векторам добавляется информация об их порядковом номере.
4.  **Трансформация (permute):** Данные подготавливаются для "старого" API PyTorch.
5.  **Контекстуализация (Encoder):** Главный этап. Через механизм **Self-Attention** каждый вектор-актив итеративно (слой за слоем) обновляется на основе информации от всех остальных векторов-активов.
6.  **Обратная Трансформация (permute):** Результат приводится к удобному формату.
7.  **Выход:** Набор векторов той же размерности, что и после проекции, но теперь каждый вектор несет в себе "суть" — обобщенную информацию о своем месте и роли во всей системе активов.

---

### 💎 Ключевой Вывод

`TransformerFeatureExtractor` — это реализация мощной парадигмы обработки наборов данных. Его ключевое преимущество, основанное на механизме **Self-Attention**, заключается в способности моделировать сложные, нелинейные взаимосвязи между всеми элементами входного набора *параллельно*. В отличие от RNN, он не страдает от "забывания" информации в длинных последовательностях, а в отличие от MLP — он не обрабатывает элементы изолированно. Он создает богатое, контекстуализированное представление системы, что является необходимым условием для принятия качественных решений в сложных средах, таких как финансовые рынки.

### 💰 Бизнес-Контекст

-   **Создание "Альфы":** Такая модель способна находить скрытые рыночные неэффективности и взаимосвязи между активами, которые не видны при простом попарном корреляционном анализе. Например, она может обнаружить, что рост актива А и падение актива Б являются опережающим индикатором для роста актива В, даже если прямой корреляции между ними нет. Это прямой путь к генерации торговых сигналов с положительным математическим ожиданием (альфа).
-   **Управление Рисками:** Вместо того чтобы оценивать риск портфеля как сумму рисков его компонентов, эта модель оценивает риск системы в целом, учитывая сложные взаимозависимости. Это позволяет строить более устойчивые и диверсифицированные портфели.
-   **Вычислительные Затраты:** Следует помнить, что сложность self-attention квадратично зависит от количества активов (`O(n_comps^2 * d_model)`). Для портфеля из 50-100 активов это приемлемо, но для анализа тысяч акций потребуются более эффективные аппроксимации Трансформера (например, Linformer, Performer).

### 💡 Катализатор Знаний

-   **Трансформеры вне NLP:** Архитектура Трансформера оказалась настолько универсальной, что теперь доминирует не только в NLP (GPT, BERT), но и в компьютерном зрении (Vision Transformer, ViT), обработке видео, предсказании структуры белков (AlphaFold 2) и, как в вашем примере, в анализе временных рядов и табличных данных.
-   **Query, Key, Value:** Внутри механизма self-attention каждый входной вектор (эмбеддинг актива) создает три новых вектора: Query (Запрос), Key (Ключ) и Value (Значение). Чтобы понять, какую информацию взять от другого актива `j`, текущий актив `i` сравнивает свой Query-вектор с Key-вектором актива `j`. Сходство между ними определяет вес, с которым Value-вектор актива `j` будет добавлен к итоговому представлению актива `i`.
-   **Обучаемые Позиционные Эмбеддинги:** Вместо фиксированных синусоидальных функций (как в классическом `PositionalEncoding`), можно использовать обучаемый слой `nn.Embedding` для кодирования позиций. Модель сама выучит оптимальные векторы для каждой позиции. Это часто дает лучшие результаты на задачах, где важен порядок.

### 📚 Векторы Верификации

-   **Архитектура Трансформера:** Оригинальная научная статья Ashish Vaswani et al., **"Attention Is All You Need"**.
-   **`torch.nn.Module`:** Официальная документация PyTorch.
-   **`torch.nn.TransformerEncoder`:** Официальная документация PyTorch.
-   **`torch.Tensor.permute`:** Официальная документация PyTorch.
-   **Визуализация Трансформеров:** Статья Jay Alammar "The Illustrated Transformer" для интуитивного понимания.
